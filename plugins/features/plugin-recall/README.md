# @napgram/plugin-recall

Deprecated compatibility wrapper for the legacy recall feature plugin.

NapGram now wires recall synchronization through the host-managed `FeatureManager`, so new deployments should not load this plugin by default.

This package is kept for one compatibility cycle only.
