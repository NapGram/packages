# @napgram/plugin-media

Deprecated compatibility wrapper for the legacy media feature plugin.

NapGram now wires media through the host-managed `FeatureManager`, so new deployments should not load this plugin by default.

This package is kept for one compatibility cycle only.
