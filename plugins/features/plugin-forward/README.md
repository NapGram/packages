# @napgram/plugin-forward

Deprecated compatibility wrapper for the legacy forward feature plugin.

NapGram now wires forwarding through the host-managed `FeatureManager`, so new deployments should not load this plugin by default.

This package is kept for one compatibility cycle only.
