---
'@napgram/web-interfaces': patch
---

Stop importing runtime helpers through `@napgram/runtime-kit/legacy` inside `@napgram/web-interfaces`, and use the direct `infra-kit` / `runtime-kit` entrypoints instead.
