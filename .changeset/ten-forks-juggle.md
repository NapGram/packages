---
'@napgram/web-interfaces': patch
---

Use `InstanceRegistry` instead of the deprecated legacy instance list in web routes, and move runtime `infra-kit`/`runtime-kit` imports into published dependencies.
